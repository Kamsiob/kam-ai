<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything. This theme exists only so the window and the
         splash screen are the right colour before the first frame lands. -->
    <style name="Theme.KamAI" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_mark</item>
        <item name="postSplashScreenTheme">@style/Theme.KamAI.Main</item>
    </style>

    <style name="Theme.KamAI.Main" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
